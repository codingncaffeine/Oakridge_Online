{
  "name": "oakridge-online",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Browser MMO: an authoritative tick-based world server and a low-poly WebGL client",
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc -p tsconfig.json && tsc -p src/client/tsconfig.json",
    "test": "node --experimental-strip-types --no-warnings=ExperimentalWarning --test tests/*.test.ts"
  },
  "engines": {
    "node": ">=22.13"
  },
  "dependencies": {
    "qrcode-generator": "^2.0.4",
    "three": "^0.186.0",
    "ws": "^8.21.3"
  },
  "devDependencies": {
    "@types/node": "^22.20.4",
    "@types/three": "^0.186.0",
    "@types/ws": "^8.18.1",
    "esbuild": "^0.28.2",
    "typescript": "^7.0.2"
  }
}
