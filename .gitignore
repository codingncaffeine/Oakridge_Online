node_modules/
dist/
data/
local-notes/
*.log

# Reference screenshots kept next to the project: never committed.
/*.webp
/*.png
/*.jpg
/*.jpeg

# Test-account secrets for checks against running sites (this machine only).
/tools/.test-accounts.json

# Snapshots, probes and temporary files from local checks (this machine only).
/snapshots/
/scratch/

# Source art and audio the shipped copies are made from (public/images, src/client/sounds).
/logos/
/sounds/
